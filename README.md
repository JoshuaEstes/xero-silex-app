# Installation

```bash
git clone URL path/to
cd path/to
composer install
vagrant up
```

# Configuration

See `config/prod.php` for settings.

# Usage

```bash
cd web/
run web server
vagrant shart
```
